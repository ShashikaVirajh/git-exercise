export type User = {
    id: number;
    name: string;
    mobile: string;
    email: string;
}

export type Product = {
    id: number;
    name: string;
    description: string;
    price: number;
}